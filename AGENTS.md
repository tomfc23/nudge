# Nudge agent instructions

When installing Nudge for a user, follow `INSTALL.md`. Before making changes,
ask whether they want the plugin and server only, or the plugin plus the
`nudge-agent` CLI. If they want the CLI, ask whether to use npm or Homebrew.
Install the CLI only when requested; the plugin works without it.
